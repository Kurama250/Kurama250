```html
<h1>Hello,</h1>

<h3>I'm Kurama, I sometimes for fun, I don't often give the codes but sometimes it happens to me.</h3>
```
```lua
local function Kurama(code)
    local Lua = Kurama.GetAll()
    local Glua = Kurama.GetAll()
    local HTML = Kurama.GetAll()
    local CSS = Kurama.GetAll()
    local Python = Kurama.GetAll()
    local PHP = Kurama.GetAll()
    local JS = Kurama.GetAll()
end
```
```css
.MyProjects {
    @import url("https://discord.gg/havenfr");
    @import url("https://discord.gg/kurama");
    @import url("https://temple-kurama.com/");
    @import url("https://virtualserv.fr");
    @import url("https://lesheitan.net");
    @import url("https://memocord.me");
}
```
